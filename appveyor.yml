version: '{build}'
branches:
  only:
    - master
    - dev
image: 
  - Visual Studio 2019
  - Ubuntu

environment:
  APPVEYOR_YML_DISABLE_PS_LINUX: true

build_script:
- ps: ./build.ps1
- sh: ./build.sh